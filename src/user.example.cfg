;; Star Citizen User Configuration
;; Copy this file to your StarCitizen\LIVE folder and rename it to user.cfg

;; REQUIRED FOR LOCALIZATION
;; This tells Star Citizen to load the custom english localization file
g_language = english

;; OPTIONAL SETTINGS BELOW
;; Uncomment (remove the ;;) any settings you want to use

;; Performance Settings
;; r_DisplayInfo = 3  ;; Show FPS counter (1, 2, or 3 for different levels)
;; r_VSync = 0        ;; Disable VSync

;; Graphics Settings
;; r_MotionBlur = 0   ;; Disable motion blur
;; r_DepthOfField = 0 ;; Disable depth of field

;; Network Settings
;; r_TexturesStreamPoolSize = 3072  ;; Texture streaming (in MB, adjust for your VRAM)

;; Controls
;; pp_RebindKeys = 1  ;; Allow key rebinding
